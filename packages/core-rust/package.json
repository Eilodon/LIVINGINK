{
    "name": "core-rust",
    "version": "0.1.0",
    "scripts": {
        "build": "pnpm build:web && pnpm build:node",
        "build:web": "wasm-pack build --target web --out-dir ../../apps/client-ngu-hanh/public/wasm --no-typescript",
        "build:node": "wasm-pack build --target nodejs --out-dir pkg-node",
        "test": "cargo test"
    },
    "devDependencies": {
        "wasm-pack": "^0.12.1"
    }
}